let
    type R = {a:int, b:string, c:R}
    type A = array of int
    type i = int
    var j:A := A[2] of 10 
    var x:i := 1
    var y:string := "hola, que tal?"
    function f(a:R, b:i, c:A):int = 1
in
  let var x:int := 2 in
    for j:=1 to 10 do let var j:=2 in x+2 end
  end
end
